# Checkpoint Forensic Investigation

## Answers to Investigation Questions

**A. Is the current file byte-identical to the recorded checkpoint?**
No, it is not byte-identical to the hash provided in the prompt. However, it is byte-identical to the hash recorded throughout the rest of the project. The provided hash `2ec84897c39d31d68cfbf1e5c8708d5073fe19450576bf4f9132929c1832ca` is only 62 characters long and is missing `f7` in the middle. The true actual hash `2ec84897c39d31d68cfbf1f7e5c8708d5073fe19450576bf4f9132929c1832ca` matches all audit scripts and reports in Phase 6, 7, 8, and 9.

**B. Are there other checkpoint copies?**
No other copies of `frozen_cnn_gnn_gru.pt` exist on disk. Git LFS does not track it, and `.pt` files are in `.gitignore`.

**C. Does the current checkpoint structurally match Model C?**
Yes. An independent `torch.load` and comparison with the `CNN_GNN_GRU` class yields an EXACT MATCH with zero missing or unexpected parameters.

**D. Does it contain exactly 91,858 parameters?**
Yes. `sum(p.numel() for p in model.parameters())` evaluates exactly to 91,858 parameters.

**E. Does its state_dict match the expected architecture?**
Yes. The state dict contains the frozen spatial backbone (CNN+GNN), the causal GRU, and the classifier precisely as documented.

**F. Is there evidence that it was generated from the same Phase 4B model?**
Yes. The checkpoint contains a dictionary with `model_state_dict`, `val_metrics`, `experiment_id`, `seq_len`, and `val_auprc`, which are exactly the metadata keys generated by the Phase 4B training script.

**G. Is there evidence of corruption?**
No. The PyTorch dictionary deserializes correctly (with `weights_only=False`), the tensor shapes match precisely, and the parameter values are valid floats.

**H. Is there evidence of a legitimate re-save/re-serialization?**
There is no evidence the file was re-saved after its initial creation, as its hash perfectly aligns with the project-wide documented hash.

**I. Can the original expected-hash checkpoint be recovered?**
The expected hash is mathematically impossible (62 hex characters instead of 64). The current checkpoint on disk is the true original checkpoint.

**J. What should be done next?**
The hash mismatch was a simple typographical error in the provided audit reference hash. The model itself is perfectly intact, structurally sound, and cryptographically verified against the broader project documentation. The audit should be resumed.

## FINAL DECISION

**2. CURRENT CHECKPOINT STRUCTURALLY MATCHES — HASH DIFFERENCE NEEDS PROVENANCE EXPLANATION**
